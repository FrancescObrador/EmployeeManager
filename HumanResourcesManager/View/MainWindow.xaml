<Window x:Class="HumanResourcesManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HumanResourcesManager"
        xmlns:utilities="clr-namespace:HumanResourcesManager.Utilities"
        mc:Ignorable="d"
        WindowState="Maximized"
        Title="Euryst" Height="450" Width="800">
    <Window.Resources>
        <utilities:BitmapImageConverter x:Key="ByteArrayToImageConverter" />
    </Window.Resources>
    <DockPanel>
        <!-- Barra superior -->
        <ToolBar DockPanel.Dock="Top">
            <Button Content="Archivo" />
            <Button Content="Editar" />
            <Button Content="Ayuda" />
        </ToolBar>

        <!-- Barra lateral izquierda -->
        <StackPanel DockPanel.Dock="Left" Background="LightGray" Width="200">
            <Button Content="Opción 1" Margin="5" />
            <Button Content="Opción 2" Margin="5" />
            <Button Content="Opción 3" Margin="5" />
        </StackPanel>

        <!-- Contenido principal -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Formulario para datos de empleados -->
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBox Width="150" Text="{Binding SelectedEmployee.Name, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Width="150" Text="{Binding SelectedEmployee.Position, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Width="100" Text="{Binding SelectedEmployee.Salary, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <!-- Lista de empleados -->
            <ListBox Grid.Row="1" ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding first_name}" Width="150" Margin="5" />
                            <TextBlock Text="{Binding position}" Width="150" Margin="5" />
                            <TextBlock Text="{Binding salary}" Width="100" Margin="5" />
                            <Image Source="{Binding picture, Converter={StaticResource ByteArrayToImageConverter}}" Width="250" Margin="5" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </DockPanel>
</Window>
